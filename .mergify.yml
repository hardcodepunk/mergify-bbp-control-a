pull_request_rules:
  - name: attacker controlled unmerged config observation
    conditions:
      - author = hardcodepunk-bbp-outsider
    actions:
      comment:
        message: |
          unmerged outsider PR config executed on PR #{{number}}
