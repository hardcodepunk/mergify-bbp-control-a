pull_request_rules:
  - name: controlled direct target merge observation
    conditions:
      - number = 27
      - author = hardcodepunk-bbp-outsider
    actions:
      merge:
        method: squash
