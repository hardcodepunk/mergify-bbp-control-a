pull_request_rules:
  - name: controlled template echo observation
    conditions:
      - author = hardcodepunk-bbp-outsider
    actions:
      comment:
        message: |
          controlled template echo title={{title}}
