pull_request_rules:
  - name: controlled scope mutation observation
    conditions:
      - scope = outsider-controlled
    actions:
      comment:
        message: |
          controlled scope mutation rule fired for PR #{{number}} by {{author}}
