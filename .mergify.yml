x-label: &target_label "security-review"

pull_request_rules:
  - name: duplicate key and anchor observation
    conditions:
      - label=*target_label
      - label=security-review
    actions:
      label:
        add:
          - parsed-by-mergify
