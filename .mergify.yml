commands_restrictions:
  refresh:
    conditions:
      - sender = definitely-not-hardcodepunk-bbp

pull_request_rules:
  - name: controlled comment action
    conditions:
      - label = bbp-trigger
    actions:
      comment:
        message: |
          controlled workflow automation fired for PR #{{number}} by {{author}}
