pull_request_rules:
  - name: controlled fork author status trust observation
    conditions:
      - author = hardcodepunk-bbp-outsider
      - check-success = controlled-ci
    actions:
      comment:
        message: |
          controlled fork-author status trust rule fired for PR #{{number}} by {{author}}
