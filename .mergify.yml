pull_request_rules:
  - name: controlled status trust observation
    conditions:
      - label = bbp-status-trigger
      - check-success = controlled-ci
    actions:
      comment:
        message: |
          controlled status trust rule fired for PR #{{number}} by {{author}}
