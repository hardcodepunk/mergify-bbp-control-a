pull_request_rules:
  - name: controlled status-success trust observation
    conditions:
      - author = hardcodepunk-bbp-outsider
      - status-success = controlled-ci
    actions:
      comment:
        message: |
          controlled status-success trust rule fired for PR #{{number}} by {{author}}
